(library
 ((name protocol_version_header)
  (public_name protocol_version_header)
  (flags (:standard -safe-string))
  (libraries (core_kernel))
  (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
