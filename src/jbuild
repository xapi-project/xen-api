(library
 ((name stdio)
  (public_name stdio)
  (libraries (base base.caml))
  (flags (:standard -safe-string))
  ;; Same comment as for Base
  (preprocess no_preprocessing)
  (lint (pps (ppx_base -check-doc-comments
                    -type-conv-keep-w32=impl
                    -apply=js_style,type_conv ppxlib.runner)))
))

(jbuild_version 1)
