(test
  (name test_suite)
  (package xapi)
  (libraries
    alcotest
    certificate_check
    daily_license_check
    xapi-stdext-date
  )
  (action (run %{test} --color=always))
)

(env (_ (env-vars (ALCOTEST_COMPACT 1))))

