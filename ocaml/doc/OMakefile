OCAMLINCLUDES += +ocamldoc
BYTE_ENABLED = TRUE
OCamlLibrary(odoc_json, odoc_json)

.PHONY: clean
clean:
	
clean:
	rm -f *~ *.cmi *.cmx *.cmo *.a *.cma *.cmxa *.run *.opt *.annot *.o v6d v6testd v6d-reopen-logs
	rm -rf xapi v6d xenstored block_device_io client common datamodel 
