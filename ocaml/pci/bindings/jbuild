(library
 ((name pci_bindings)
  ;(public_name xapi.pci_bindings)
  (flags (:standard -safe-string))
  (libraries (ctypes.stubs ctypes ctypes.foreign.threaded))
  (wrapped false)
 ))

(jbuild_version 1)
