
let ( +* ) = Int64.add
let ( -* ) = Int64.sub
let ( ** ) = Int64.mul

let mib = 1024L ** 1024L
let gib = mib ** 1024L
let tib = gib ** 1024L

let mb = 1000L ** 1000L
let gb = mb ** 1000L
let tb = gb ** 1000L
