(executable
  (name main)
  (libraries
    cmdliner
    message-switch-lwt
    xapi_guard)
  (preprocess (pps ppx_deriving_rpc)))

(install
  (package varstored-guard)
  (section sbin)
  (files (main.exe as varstored-guard)))
