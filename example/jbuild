
(library
 ((name example_ppx_sexp_value_lib)
  (flags (:standard -safe-string))
  (libraries (core))
  (preprocess (pps (ppx_sexp_value ppxlib.runner)))))

(alias ((name DEFAULT) (deps (example.ml.pp))))


(jbuild_version 1)
