(jbuild_version 1)

(library
 ((name networklibs)
  (flags (:standard :standard -bin-annot -safe-string))
  (libraries (astring
              forkexec
              rpclib
              systemd
              threads
              re.perl
              xapi-stdext-unix
              xcp-inventory
              xcp.network))
  (wrapped false)
 )
)