(jbuild_version 1)

(library
 ((name networklibs)
  (flags (:standard :standard -bin-annot))
  (libraries (astring
              forkexec
              rpclib
              systemd
              threads
              xapi-stdext-unix
              xcp-inventory
              xcp.network))
  (wrapped false)
 )
)