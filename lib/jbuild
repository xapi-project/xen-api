(jbuild_version 1)

(library
 ((name networklibs)
  (flags (:standard :standard -bin-annot))
  (libraries (forkexec
              threads
              rpclib
              stdext
              xcp-inventory
              xcp.network
              systemd))
  (wrapped false)
 )
)