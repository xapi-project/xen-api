(library
  (name xcp_varstore_deprivileged)
  (public_name xcp.varstore.deprivileged)
  (modules (:standard \ varstore_deprivileged_cli))
  (flags (:standard -w -39))
  (libraries xcp threads rpclib.core xcp.xen)
  (wrapped false)
  (preprocess (pps ppx_deriving_rpc bisect_ppx -conditional)))

(executable
  (name varstore_deprivileged_cli)
  (modules varstore_deprivileged_cli)
  (libraries
    cmdliner
    rpclib.cmdliner
    rpclib.markdown
    xcp.varstore.deprivileged))

(alias
 (name   runtest)
 (deps   varstore_deprivileged_cli.exe)
 (action (run %{deps})))
