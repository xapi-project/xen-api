(jbuild_version 1)

(executable (
  (name dbgring)
  (public_name dbgring)
  (package xapi-xenopsd-xc)
  (libraries (
    xapi-xenopsd
    xenctrl
    xenstore
    xenstore.unix
    xenstore_transport
    xenstore_transport.unix
    threads
    stdext
    xcp.xen
    rpclib.core
    uutf
    xcp
    rpclib.json
  ))
))
