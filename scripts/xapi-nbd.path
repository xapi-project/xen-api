[Path]
# PathExists must match the server-cert-path in xapi.conf and 
# the --certfile option in xapi-nbd.service: any change must be made in all three files.
PathExists=/etc/xensource/xapi-ssl.pem

[Install]
WantedBy=default.target
