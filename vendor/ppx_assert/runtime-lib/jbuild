(library
  ((name ppx_assert_lib)
   (public_name ppx_assert.runtime-lib)
  (flags (:standard -safe-string))
   (libraries (base base.caml))
   (preprocess (pps (ppxlib ppx_sexp_conv ppxlib.runner)))
))


(jbuild_version 1)
