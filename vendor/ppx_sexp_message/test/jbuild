(library
 ((name test_ppx_sexp_message_lib)
  (flags (:standard -safe-string))
  (libraries (core))
  (preprocess (pps (ppx_sexp_conv ppx_sexp_message ppx_expect ppxlib.runner)))))


(jbuild_version 1)
