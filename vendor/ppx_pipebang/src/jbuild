(library
 ((name ppx_pipebang)
  (public_name ppx_pipebang)
  (kind ppx_rewriter)
  (flags (:standard -safe-string))
  (libraries (ppxlib))
  (preprocess (pps (ppxlib.metaquot ppxlib.runner)))))


(jbuild_version 1)
