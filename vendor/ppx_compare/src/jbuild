(library
 ((name ppx_compare)
  (public_name ppx_compare)
  (kind ppx_deriver)
  (flags (:standard -safe-string))
  (libraries (ppxlib
              ppx_compare_expander
              ))
  (preprocess no_preprocessing)))


(jbuild_version 1)
