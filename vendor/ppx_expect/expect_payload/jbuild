(library (
  (name ppx_expect_payload)
  (public_name ppx_expect.payload)
  (flags (:standard -safe-string))
  (libraries (expect_test_common
              ppxlib))
  (preprocess (pps (ppxlib.metaquot ppxlib.runner)))
))


(jbuild_version 1)
