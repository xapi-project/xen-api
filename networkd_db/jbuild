(executable
 ((name networkd_db)
  (public_name networkd_db)
  (package xcp-networkd)
  (flags (:standard -bin-annot -warn-error +a-27))
  (libraries (profiling
              networklibs
              stdext
              threads
              xcp.network)
  )
 )
)