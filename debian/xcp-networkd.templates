Template: xcp-xapi/networking_type
Type: select
Choices: bridge, openvswitch
Default: openvswitch
_Description: XCP networking manager:
 xcp-networkd has two modes of networking. With bridge, all of your virtual
 machines will see all traffic, and by default will be able to connect to each
 other, while openvswitch manages VLAN.
